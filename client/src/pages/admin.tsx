export function Admin() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Admin Center</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        User, group, site, storage, audit log, and settings management will appear here.
      </p>
    </div>
  );
}
